# 本项目的Codex接续说明

以下只约束本项目，不覆盖系统、工具权限或用户本轮明确指令。

1. 先按PRD第13.0节区分模式：`first_setup`（新电脑首次初始化）、`local_continue`（老电脑继续制作）、`rules_update`（用户要求更新规范／接续）。新会话、新选题、新集数、任务重试和重启Codex都不等于新电脑。用户明确指定模式时遵从；未指定而本地资料齐全时默认 `local_continue`。
2. `local_continue` 只读取本地PRD、相关spec、本期定稿与制作记录，复用现有资产，从未完成处继续。禁止为了例行开工通过Git、浏览器、API或连接器访问GitHub，禁止clone／fetch／pull／ls-remote、远端版本检查和GitHub登录探测；不以联网或取得最新main为制作前提。缺件先查本地目录，只报告具体缺件并继续独立工作，不擅自转成首次初始化。只有 `first_setup` 或已明确授权的 `rules_update` 才按PRD安全同步。唯一规范仓库为 `https://github.com/KAtOReNA7/tiktok`，主分支main；不自动丢弃、stash或强推。
3. 接续资产已在assets和refs内。含Doro的生图实际输入用户身份原图；版式截图不能用作脸型母图。用户负责剪映配音、试听和装配。
4. 每集默认40秒以内，交同模板封面和一句话简介。收到最终人声后交真实黑底白字字幕MP4，不能只交SRT。
5. 给用户的素材映射严格只有“插图编号、对应口播”两列，逐行完整原文，不含时码、路径、备注或其他列。图片文件名带同一编号；字幕时间单独处理。
6. 不生成AI视频，不提供音效、配乐、轻动效及其建议，不运行旧A/B或从零GUI整片流程。最终素材放本地 `交付/{选题ID}_{短名}/`，不压缩。
7. 武松已完成；商鞅仍待确认。李白已有采用稿，用户反馈第二集正在Codex执行；实际素材进度以本地制作记录为准。新机器须取得当前任务所需的完整定稿与执行指令；老电脑沿用本地文件，不仅凭本摘要生图、恢复旧稿或重做已完成素材。没有明确新任务时读取状态并说明待决定事项。
8. 仓库只保存PRD、项目方向、接续说明、必要状态摘要、通用模板与参考资产。选题调研、论点、口播、来源表、分镜、单集生产与审片记录和全部成品只在本地；默认本地工作根为仓库旁的tiktok-local。普通改稿不提交Git，定稿也不自动上传。只有规范／接续实际变化时才更新关联文件、刷新manifest、校验、提交并正常push。受保护分支用PR，不绕过保护。
9. 仅在规范更新任务中，远端读回成功后再报告同步完成和提交号。日常制作交付不附带远端检查或提交任务。当前环境无法写入时保留本地成果并说明阻塞，不声称会在后台自动上传。不自动发布抖音、购买额度或发送账号消息。


10. 评价广泛人设时，用多件具体事例快列或按多个故事分期；关键人物点明姓名、身份和关系，不靠一个冷门故事反复解释凑时长。
