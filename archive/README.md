# 历史归档：不作为执行入口

这里保存原V1.1模板参数与Figma创建记录，证明坐标和节点来源。里面的production、selection、voice_script_status、示例标题、秦始皇章节以及旧路径是当时状态，不能当成下一选题任务或当前已验证能力。

当前规则看PRD_V7.md（V7.1），当前参数看spec/template.json。不要自动回读旧SOP，不重新制作秦始皇，不把无字生图旧要求恢复。

obsolete_sfx_cues_V7_0.csv为已取消的旧字段，仅存档；不能创建或提供音效建议。旧压缩包是历史文件，不作为V7.1新交付物。
