# 古今人物锐评短视频｜统一 PRD V7.2

版本日期：2026-09-18；本版新增GitHub统一接续与同步规则，保留V7.1制作规则和两列映射要求  
文档用途：新电脑、新 Codex 会话接续下一选题的完整制作依据。  
当前主流程：**统一选题与40秒内分集口播 → 漫画分镜 → 素材、每集封面与一句话简介 → 黑底字幕视频 → 用户在剪映装配 → 局部审修。**  
当前画面模板：`RYP_WHITE_SINGLE_V1_1`。PRD 升至 V7，不代表 Figma 模板也升至 V7。

> V7.2统一由 https://github.com/KAtOReNA7/tiktok 的 `main` 分支持续维护。新电脑先按第13.1节同步仓库，再读取本版和 `START_HERE.md`。当前进度见 `PROJECT_STATE.md`；规范版本、Figma模板版本、单集素材版本分别管理。后续更新直接修改仓库，不重复制作新的接续压缩包。

> V7.1已整合用户新增七条要求：不制作或提供音效、配乐与轻动效；字幕默认交黑底MP4；每集默认40秒以内；每集独立结尾，片头默认独立、合适时同选题共用；每集一张同模板封面和一句话简介；素材集中在有清楚命名的文件夹内，不压缩。此前V7.0中冲突条款已替换。本版同时保留历版有效要求、Doro纠偏、漫画规范和后续审片经验。新会话读取本版、`START_HERE.md` 和 `PROJECT_STATE.md`，无需重读全部历史聊天或逐个拼补丁。本版不代表远端Figma或用户新电脑工程已同步修改。

## 0. 五分钟接手

### 0.1 项目是什么

制作面向抖音的9:16历史／古今人物与事件锐评短视频。核心是高知名度对象、强烈逆向评价、连续具体事例、网络化口播和有表现力的漫画，争取观看和讨论。它不是百科讲解，也不是只输出情绪的骂人视频。

画面沿用白底、黑灰文字、少量荧光黄和朱印的系列模板。漫画融合历史画像／木版画质感、二次元夸张表情和拼贴语言。咕咕嘎嘎、Doro、曼波是角色资产，按选题换历史相关服装；边缘装饰与正文漫画分层。

### 0.2 当前明确分工

| 工作 | 默认负责方 | 本次接续要求 |
|---|---|---|
| 选题、观点、口播取舍 | 本工作窗口与用户统一决定，Codex 可调研和提案 | 已有定稿就执行；不能自行换立场或补回删掉的段落 |
| 分镜、人物设定、漫画与局部编辑 | Codex | 交付真实文件，不停在提示词 |
| 固定模板、套版场景图、透明角色层 | Codex 复用已有资产 | 内容替换不等于重新设计UI |
| 曼波配音生成与最终试听 | 用户默认在剪映操作；另有明确委托再交Codex | Codex 准备纯口播稿与分段文本 |
| 字幕文本、断句、实际时码、黑底字幕视频、缺失字幕补齐 | Codex | 依据最终配音／当前成片交付已排版的黑底MP4；用户对齐到黑色字幕区域 |
| 每集封面和一句话简介 | Codex | 封面使用同一视频模板，漫画构图有张力；简介针对该集 |
| 剪映剪辑、编号顺序、边缘UI | 用户 | Codex交付清楚素材映射，不提供音效、配乐、轻动效及其方案 |
| 成片审查与局部返修素材 | Codex／本工作窗口 | 标时间、区分技术错误与创意选择；听审能力不足如实标注 |
| 发布与账号操作 | 用户 | 素材制作不包含自动发布、购买额度或账号互动 |

### 0.3 已退出默认流程

- AI视频、Seedance、Sora、Runway等视频生成：因成本取消，不作为必需素材，也不持续探查入口。
- 剪映助手：用户实测不满意，不再使用。
- 旧A/B测试、每次从零搭剪映整片、反复跨机重开测试：本轮不继承。
- 每次生成前重新汇总PRD、搭第二套渲染框架、安装一整套操作SOP：不作为开工前置条件。
- 旧0.8倍慢配音：废止。当前为原声只处理一次到1.25倍。
- “所有图片无字，文字全部后期简单叠上去”：废止。场景文字和漫画台词按第7节制作；标题、出处和口播字幕仍独立排版。
- 音效、配乐与轻动效：不再制作或提供，连同搜索词、落点表、推荐曲目、关键帧方案一并取消。
- 仅交SRT作为字幕成品：取消。黑底字幕视频是默认交付，SRT仅可作为辅助或制作中间文件。
- 素材压缩包：取消。后续素材交付使用统一文件夹，清楚命名与版本映射。
- 每集不固定20／24张图或固定三集；时长现在默认40秒以内，包含片头、正文、片尾和停顿。

## 1. 权威顺序与版本管理

1. 用户本轮明确指令、用户确认的定稿和当前成片版本优先。
2. GitHub仓库 `KAtOReNA7/tiktok` 的 `main` 分支中，本 PRD V7.2 为唯一现行主规范；文件名仍为PRD_V7.md，避免产生多份入口。本地旧接续包是快照，以同步后的仓库为准。
3. 当前选题的 `episode/brief.yaml`、定稿、分镜及明确限定范围的任务单补充本版。
4. `spec/template.json` 定义目前继承的画面尺寸与颜色；如用户提供更新模板，以其为准并记录差异。
5. `archive/`、秦始皇旧稿、旧PRD与旧SOP只用于追溯，不能自动恢复旧任务。

本顺序用于处理项目资料之间的冲突，不用于绕过当前环境的权限、工具限制或更高层指令。旧 README／AGENTS 中已过期的项目流程应按用户本次授权标为废止或指向 V7；不删除无关工程要求。

新选题使用新目录和新ID，旧素材只作可复用资产／参考，不覆盖已交付文件。已有同一规范不再复制成多份“最终版”。临时补丁收口后只把通用规则并入主PRD；本期时间点、镜头数和剪辑限制留在本期记录。

## 2. 内容定位与选题

### 2.1 什么样的反调值得做

优先挑大众熟悉且公共形象相对稳定的对象：被广泛推崇的英雄、名君、名人，或广泛遭骂的奸臣、昏君、反派。用具体行为、结果、能力或代价挑战既有印象。古今中外与文学人物都可进入，但要明确属于历史人物还是作品角色。

知名度、形象反差、可展开的具体材料和漫画可表现性同时考虑。不能只找一个冷门人物说他“其实不行”；本来就褒贬混杂的对象，只有发现足够强的新角度才优先。

不把“唱反调”理解成只沿用用户曾列过的名字。没有指定选题时，主动检索抖音、B站等公开可访问的热门内容和讨论，寻找独立的新候选；平台内容无法访问就说明范围，不编造热度、点赞数或评论共识。记录可见标题、链接、日期、争议点；搜索热度只支持传播判断，不自动支持历史结论。

### 2.2 候选交付

默认先给约6个有区别的候选，而不是几十个泛泛题目。每个包含：

| 字段 | 内容 |
|---|---|
| 对象与大众印象 | 大家通常如何理解此人／事件，依据来自哪里 |
| 反向主张 | 本片只想让观众记住的一句话 |
| 标题 | 直接、有网感、体现冲突；不写成论文题目 |
| 支撑材料 | 至少能落到具体行为、结果、数字、故事或评论的线索 |
| 来源类别 | 正史、笔记、野史、文学、现代文章、视频等 |
| 画面潜力 | 至少一处可形成特写、动作、反应或视觉比喻的事件 |
| 当前缺口 | 哪项数字、归属或原文还需要核对 |

已指定选题时跳过大范围选题推荐，直接围绕该题推进。下一选题未指定时，不把“接续”误解为继续秦始皇或自动选关羽量产。

### 2.3 材料范围和使用方式

正史、野史、笔记、八卦、公众号文章、小说、抖音/B站高赞视频都可用于发现角度和建立表达。本项目优先寻找能够支撑所选观点的具体材料，不要求每条片平均呈现双方意见，也不按学术论文展开考据。

同时保持最基本的可追溯性：野史故事写明其来源；小说情节不能标成正史；漫画自拟台词不能放进古人原话的引号。不能编造原文、出处、统计比例或视频热度。对暂时无法确认的关键材料，保留为线索，换用有支撑的表达，不靠补造事实填满稿子。

在后台 `sources.csv` 记录来源类别、作品或作者、章节／页面／视频位置、链接、可支持的具体句子。前台只显示短出处，不把核查过程塞进口播。转载相同材料不当成多个独立证据。

数字类标题须说明口径。例如“胜率不到20%”必须先明确何为一次战役、是否独立指挥、胜负不明如何处理；用户给过的此类标题是力度示例，不是现成的已核实数据。

## 3. 标题与曼波口播

### 3.1 标题

直接给判断或反差，人物必须易识别。允许强烈、土、网络化的语言，不把标题磨成“重新审视某某的复杂性”。可采用“名号与实际表现的落差”“被谁抬上神坛”“能力究竟配不配”“坏名声掩盖了哪项能力”等结构，但每次落实到具体对象，不机械套“最被高估”。

既有力度参照包括“被罗贯中代练上分，诸葛亮智力真配100吗？”等用户表达；这些用于理解口吻，不要求复用，也不视为选题已经获批。秦始皇本期“高估→过誉”的修改是该期确定内容，不强制所有新标题都出现“过誉”。

### 3.2 文案结构

1. 开头直接亮暴论／结论，尽快点出人物和冲突，不以背景铺垫或“今天带大家了解”开场。
2. 用具体事件、处理方式、后果、数字或名人评语连续支撑。每推进一句，尽量增加信息。
3. 接短白话解释或笑点，让观众马上理解这个事例为什么支撑判断。
4. 结尾收束本片观点，必要时自然接下一集；不强制互动提问、反例或平衡段。

“只能评级理由”的意思：做排名时给清楚判档依据；做单观点视频时围绕该观点展开，不强行改成双方辩论。尖锐来自具体内容，不靠连续对观众说“你还吹”“你姓嬴吗”凑时长。

### 3.3 语言要求

- 短句、口语、直接、网络化。古文引语后用短白话解释，不整段文绉绉。
- 不出现用户已排除的政治人物、政治宣传讨论和“燃向混剪”等AI味词语来解释人物口碑变化。
- 不写空泛的价值升华，不强行承认反方，不在每个观点后接“当然，也不能忽视……”稀释主张。
- 保留对象、动作、数量单位和因果关系；简化语言不能把“户”改成“人”，或把不同事件拼成同一件事。
- 历史人物原话、后人评论、文学讽刺和本片吐槽在资料中分清。
- 口播文字、字幕文字、漫画台词是不同层。图内台词不自动成为新的配音。

### 3.4 文稿交付和锁定

一份定稿包含标题、核心主张、按内容段划分的口播、短出处和必要读音备注。另交纯正文 `voice_script.txt`，去掉章节编号、URL、表格、配音备注。

仅确有TTS误读时提供 `tts_text` 与 `display_text` 对照；字幕仍保持规范人名、汉字和单位。不要把为发音准备的同音字直接变成观众看到的字幕。

用户确认定稿／已开始配音后，画面优化不得自行删稿、换观点、改片头片尾或追加口播。用户明确要求改稿时，只更新受影响段并记录版本。

## 4. 每集40秒内与系列片头片尾

### 4.1 默认时长

每集最终成片默认不超过40.00秒，包含片头、正文、结尾与停顿。以原声只处理一次到1.25倍后的实际声音为准；不能只算正文40秒，再额外加片头片尾。30fps时间轴的默认上限为1200帧，不靠再次加速规避。

新选题从写稿开始按这个容量组织。内容过多先减少重复或按独立问题分集，不能先写长稿后按秒数机械截断。初稿时长只能标估算，收到最终人声后再核对实际长度。若用户另行指定时长，以本轮指令为准；已锁定超时稿件先提出精简／分集方案，不静默删改定稿。

### 4.2 每集完整表达

- 每集有自己的论点、开头、正文和结尾；正文能单独理解，不依赖观众按顺序看过前集。
- 每集默认分别写开头和结尾；开头可按4.3共用，结尾仍针对本集独立收束。
- 每集结尾讲清本集结论，必要时短预告下一集；末集完成整个选题的收束。不得只留下半句等待下一集。
- 一句名人评论可以服务本集结尾，不要求单独再加一段长总结。预告也计入40秒。
- 不把所有评论者集中到末集，不把拆分固定为三集；集数服从内容和40秒容量。
- 人声、字幕和画面按完整语句与内容段共同组织；集内章节从01开始，主题ID、集号与镜头ID分开。
- 新分集时间轴分别从0开始；对已有单集局部补字幕，映射回该单集的原时间轴。

### 4.3 同选题共用优秀片头的例外

如果选题的开头已经足够有吸引力，而且对所有分集都成立，可在该选题各集使用同一个开头，不必为每集强行重写。

共用条件：开头不依赖上一集，不预告本集没有的内容，人物和核心主张适用于各分集。brief记录intro_mode为shared_topic，并列明共用片头文件与适用集号；否则使用per_part。

共用的是批准的片头文字，以及实际已有、可复用的配音和画面。只制作一份共用片头资产，逐集映射，避免重复生图或配音；每集仍计入该片头时长，字幕时码以本集最终声音为准。共用片头后直接进入该集具体事件。

每集仍各自提供封面、简介和结尾。共用片头不等于共用整集内容或复用同一个片尾。用户自行调整的片头片尾为准，技术审查不恢复旧建议。

秦始皇第五版40.37、42.43、60.80秒属于旧历史样例，不再作为新选题默认长度。当前40秒来自用户制作策略，不声称是平台推荐门槛。

“从夯到拉”保留为可选形式：夯、顶级、人上人、NPC、拉完了。排名仍需统一维度；当前白底单格模板没有已验证的完整榜单组件，不能声称现成可用。

## 5. 固定画面模板与 Figma 资产

### 5.1 已有资产

- Figma资产页：https://www.figma.com/design/gc9VEwuG01X8ZsMTHg7HsQ?node-id=50-2
- 锁定母版：节点 `50:60`。
- 制作副本：节点 `50:4`，每期从副本复制；固定底板节点 `43:7`。
- 本包含1080×1920底板、三位秦朝造型透明PNG、整屏透明角色层、模板参数、Doro原图和画面示例。

上述链接与节点来自已保存的制作档案，本次未重新访问远端验证或修改。原档记录为独立资产页和锁定母版，未声称已经保存命名版本历史或发布团队组件库。

Figma用于固定模板的精确编辑与导出。没有Figma入口时，仍可利用现有底板和参数完成文件准备、漫画、字幕与确定性套版；确实不能套版则交独立素材及映射，标清缺件，不长时间找入口，也不冒称已更新Figma。

### 5.2 设计语言

保持用户批准的终末地UI参考方向：中性白底、黑灰高对比文字、细线和刻度、少量荧光黄点缀。古风进入漫画纸纹、服饰、道具和朱印；不把整屏改回淡绿色或泛黄纸底。

保留朱印。上方小标题用于事件或章节，不恢复“咕咕嘎嘎 / DORO / 曼波”三人名字栏。账号最终名称与主页装修未在本版重新定案，不把模板中的品牌字样当作账号名称已完成注册。

### 5.3 精确坐标

单位px，原点为1080×1920画布左上角。以下为已有V1.1设计基准；若收到用户最终剪映样式或新模板，以其为准记录校准差异。

| 元素 | x / y / 宽 / 高 | 字体与样式 |
|---|---|---|
| 章节小标题 | 96 / 248 / 776 / 38 | Noto Sans SC Bold，30px，行高36，#303030 |
| 主标题 | 96 / 306 / 776 / 176 | Noto Sans SC Black，72px，行高88，最多2行，#161616 |
| 期数标签文字 | 142 / 492 / 160 / 34 | Noto Sans SC Bold，28px，行高34，#161616；长集号需在既有标签可用范围内校准 |
| 漫画外框 | 128 / 536 / 744 / 752 | 3px内描边，按窗口裁切 |
| 漫画内框参考 | 131 / 539 / 738 / 746 | 底板分层装配时避免盖住边框 |
| 短出处 | 128 / 1336 / 620 / 40 | Noto Sans SC Bold，32px，行高40，#3D3D3D |
| 黑色字幕底 | 96 / 1408 / 776 / 128 | #1C1C1C |
| 字幕文字框 | 120 / 1420 / 728 / 104 | Noto Sans SC Bold，44px，行高52，白色，最多2行 |
| 页脚 | 0 / 1660 / 1080 / 260 | #F0F0F0，品牌字#595959 |

背景#FAFAFA；强调#F2FA00；朱印#AA352D；细刻度#D4D4D4。正文文字100%不透明。朱印用Noto Serif SC，已在固定底板中保留。

标题每行约10个全角字、字幕每行约16个全角字只作长度估算，最终按实际渲染宽度检查。超长先断句或精简，不能自动缩成小字。标题强调色块是独立层，随关键词位置变化，不硬编码每期都落在秦始皇示例的坐标。

44px是画布设计字号，不保证等于剪映面板的“44”。新电脑用一条最长的两行字幕在1080画布中对齐文字框后保存样式；缺同名字体时优先使用可用的等效粗体并做视觉匹配，记录实际字体，不能偷偷换成细体。

### 5.4 安全区

当前关键内容区为 x=96—872、y=240—1536。顶部240px、右侧208px、底部384px为项目预留，不是所有抖音设备通用的官方固定遮挡范围。

核心标题、人脸、证据文字、短出处和字幕留在关键区。边缘角色可以被实际应用UI遮挡，不承担关键信息。角色浮在“视频自己的UI”之上，不能浮到抖音应用界面的点赞、评论按钮之上。

新设备或界面样式变化时，用一次真实手机预览校准；不因尚未实机验证而自行推翻模板。

## 6. 三位角色、cos与一致性

### 6.1 每期cos

三位保留自身可辨认的二次元身份，随主题扮演故事相关人物或角色。秦始皇样例为Doro扮秦始皇、咕咕嘎嘎扮传令官、曼波扮劳役者。关羽题可使用刘关张组合；项羽题可使用项羽、虞姬、拍照者的喜剧组合。这些是选角范例，新题具体分工写入本期设定。

正文每幅漫画不强制三位同框。需要普通文士、士卒、百姓时按情境画，不把所有历史人物都替换成同一张脸。

### 6.2 Doro身份第一参考

`refs/identity/doro_primary_user.png` 为用户原始参考，优先级高于任何生成图。`doro_expression_user.png` 仅参考右下角粉发Doro的哭泣表情和身体，不参考领奖台上的其他角色。

关键特征：宽圆脸、粉色分束刘海、侧边玫瑰卷与蝴蝶结、大而简洁的紫眼、短小嘴部、白色短肢身体。避免尖下巴、写实鼻梁、复杂美少女眼和普通粉发Q版人类身体。

每次生成／编辑含Doro的图片，实际向图片工具传入原始参考。不能仅在提示词写文件名。多图分别标明身份、服装、风格、构图、编辑目标；只支持一张图时先完成可靠的cos校准图。原图无法输入就明确能力缺口，不能把纯文字生成叫“参考图编辑”。

身份一致不等于表情和姿势固定。愤怒、哭泣、侧脸、夸张透视都可用；不能为“像Doro”把每幅都改成同一笑脸。不能让上一张偏脸的生成图成为下一张唯一身份参考。

### 6.3 边缘角色

| 角色 | 带透明留白的图层框 x / y / 宽 / 高 | 动作 |
|---|---|---|
| Doro | 936 / 96 / 180 / 180 | 右外缘直立探身；不倒挂，不放大成标题旁大头像 |
| 咕咕嘎嘎 | -30 / 784 / 180 / 180 | 左外缘扒住，身体部分在画布之外 |
| 曼波 | 128 / 1785 / 150 / 150 | 底边露头和手；不遮挡“锐评”品牌字 |

坐标针对原640×640透明画布，不能先按非透明内容自动裁边，再套相同尺寸，否则大小和落点会变。角色位于实际1080×1920画布边缘，不是漫画窗边缘，也不是内部x≈978的竖线边缘。

新cos按这些姿势重新设计服饰、头饰和手部关系，不把站立全身图硬缩小塞上去。输出真正透明PNG，NORMAL混合，不使用棋盘格假透明。三个角色整体视觉体量接近当前咕咕嘎嘎，不挤成一排，不占正文排版空间。

同主题复用已确认的整屏透明叠加层；不同主题只更新cos内容，保持锚点与大小。检查透明父层、蒙版、缩放和合成尺寸，防止第五版1.3出现的Doro内侧切断、左侧残边和底部角色消失。

## 7. 漫画画面生产规范（已合并 COMIC-VISUAL-01）

### 7.1 先设计分镜，再生图

每个镜头须有明确视觉任务：交代事件、证据、动作、反应、结果、对照或笑点。不能只是把字幕描述改写成一张图。

分镜至少包含：镜头ID、对应定稿口播、视觉任务、动作瞬间、景别与角度、主体比例与位置、前后景、图内文字与承载位置、相邻镜头变化、实际参考文件。素材生成阶段按插图编号与完整口播原文建立对应，不设置时码列；字幕时码留到最终声音确认后单独制作。

### 7.2 镜头和主次

- 根据内容选择全景、中景、近景、局部特写、俯视、仰视、主观视角、前景遮挡、反应镜头和夸张透视，不机械轮换。
- 连续三张若景别、主体位置、动作都相同且没有叙事理由，调整至少一张。
- 允许裁切角色、偏置、背向、侧脸、只画眼睛或手与道具。不要每张都全身正面站在场景中央。
- 一个主要视觉焦点。人物表情、关键动作和关键物件优先，背景可以简化为色块、线条或少量环境。
- 动感来自重心、动作线、透视、衣物与道具的方向；速度线、碎屑和拟声词只是辅助。
- 主体大小不是每张都相同：证据格可以让石刻占大部分画面，反应格放大面部，大场面用一个前景人物建立感受。
- 保持线条、设色和有限纸纹统一，不要求每张都有同等精细的山水、建筑与人群。
- 单格是当前已确认主版式。漫画内部必要时可设计简单对照，但新增整屏两格／证据卡UI须另行明确，不能擅自把未定稿版式当母版。
- 可以借鉴漫画、插画、广告画的镜头和视觉引导技巧，明确写成比例、视角、动作与阅读顺序；不能只写一个画家名字代替设计。

### 7.3 三类文字分开制作

| 文字类型 | 生成及排版方式 | 验收重点 |
|---|---|---|
| 石刻、卷轴、牌匾、旗帜等场景文字 | 生图前写出准确文字和承载方式，优先随场景生成；错误时局部修复，必要时做匹配材质透视的精确植字 | 内容、笔画、透视、墨迹／刻槽／阴影都正确；普通黑体平贴不算完成 |
| 对话、内心、拟声词 | 先设计气泡／文字位置、指向和阅读顺序；短句可随图生成或后期精确填字 | 说话者归属清楚，手机上能读，不重复整段口播 |
| 主标题、小标题、短出处、口播字幕 | 模板或字幕系统确定性排版 | 可修改、准确、稳定，不由生图模型重画 |

每张通常0—1句短台词、约2—8字；不是硬性字数，必要的准确引文不错误截断。台词提供角色视角或反差，不给每张强塞对白。拟写台词在资料中标“剧情化台词”，不要冒充古人原话。

图内文字同时验收“写对”和“长在画面里”。OCR只能辅助校字，不能代替看透视、材质与气泡关系。观众在原镜头时长内读不完时，先缩短或删去图内台词，不擅自延长已锁时间轴。

### 7.4 参考图和修改方式

身份参考锁定角色；服装参考锁定本期cos；画风参考提供线条色彩；构图参考提供镜头关系。不能把四种职责混为一张旧图。

单独脸、文字、手部出错，优先局部编辑。构图、动作和相邻镜头关系同时有问题，再完整重绘。不同评论者不能只换卷轴上的字，继续复用同一张脸、托腮姿势和桌案。

旧图裁切可以作为明确标注的复用；不能冒称已经新生成了动作、表情或机位。需要新图时用真实图片生成能力，不用程序绘制的占位小人、截图贴字冒充合格漫画。

### 7.5 尺寸与裁切

漫画目标画窗744×752，接近正方形。优先生成较高分辨率近正方形源图，1536×1536仅为可用时的目标，不伪称工具支持。保留实际原图和实际尺寸，适配窗口时检查主体、关键字与动作。

关键区域预留适当裁切余量；余量不等于必须完整保留人物全身。特写有意裁掉身体是镜头设计，裁掉关键字或表情则是缺陷。独立插画不能含外层标题、字幕黑框、朱印、品牌或页边角色。

### 7.6 生产节奏

先完成少量有代表性的镜头，如身份近景、动作格、复杂场景，检查画窗效果后继续。同一身份已经稳定时不每张等待批准；已发生用户指出的身份偏差时，先给一张纠偏对照，等待期间做不依赖该角色的工作。

每图先出一稿，针对具体缺陷修改，不批量随机抽卡。连续修正仍不合格时记录问题并调整构图或交用户判断，不无限重复。保留实际提示词、参考用途和文件版本；不另造大量同义报告。

## 8. 可直接使用的提示词结构

以下是填写模板，不是已经生成的素材。

### 8.1 新漫画

```text
用途：9:16历史锐评视频中744×752的独立漫画窗口，输出近正方形插画。
本镜头ID：{shot_id}。对应定稿口播：{verbatim_voice_line}。
视觉任务：{事件/证据/动作/反应/结果/对照}。
动作瞬间：{谁在做什么；动作方向；关键物件；不可画错的关系}。
镜头：{景别、视角、主体位置比例、前后景、唯一主要焦点}。
与上一镜区别：{景别/视角/动作/信息增量}。
图A为角色身份原图，图B只参考本期服饰，图C只参考线条和设色。
古风画像/木版画线条、有限纸纹、二次元夸张表情和拼贴感；背景为叙事服务。
场景文字：{准确文字、承载物、透视材质；无则写无}。
漫画台词：{短句、说话者、气泡位置；无则写无}。
保留角色身份，允许当前情绪和镜头透视；不要复制偏掉的脸。
只输出插画，不生成外层UI、标题、出处、口播字幕或水印。
```

### 8.2 场景文字局部修复

```text
编辑目标：{实际问题原图}。
仅修复{承载物/区域}上的文字为“{准确文字}”。
让笔画随纸面/石面透视，匹配{墨色渗入/刻槽/磨损/阴影}。
保留其他人物、动作、构图、道具与色调。不要覆盖普通印刷黑体充当题字。
```

### 8.3 新选题页边cos

```text
图A是确认的原始身份参考；图B是既有扒边动作与透明画布参考。
角色{名称}扮演{本期角色}，服饰道具为{设定}。
保留图A身份和图B扒画布外缘的姿势、身体露出比例、大小与朝向。
不是站立全身图，不倒挂，不重新设计版式。
输出独立透明PNG，保留与模板相容的透明留白；不要棋盘格、文字或UI。
```

## 9. 套版、素材层与用户装配

### 9.1 两种交付方式都要理解

**快速硬切方式：** 导出1080×1920 `scene_plates/` 场景图，包含固定UI、当段插画、小标题、短出处、标题强调与空黑字幕底；隐藏所有示例口播字幕和页边角色。用户在剪映切换场景图，再叠字幕和一层整屏透明角色。

**独立素材方式：** 保留独立底板、漫画以及必要的标题／出处，便于用户替换和装配。现有底板是整屏不透明PNG，放在漫画下面，不能盖住漫画。此方式不附带轻动效、关键帧或转场制作任务。

根据当前用户工程沿用匹配方式，不强迫用户重建图层。独立漫画必须保留，即使同时交整场景图，也不能只留下无法调整的扁平整屏图。

### 9.2 图层关系

从下到上：底板／场景图 → 独立漫画（分层方式才加）→ 必要道具与重点文字 → 未烘焙的标题／出处 → 口播字幕 → 页边角色透明层。声音交接只涉及既定人声；不交付音效、配乐或混音方案。

已在场景图里的内容不要重复叠加。静态场景图绝不烧入某一句口播，否则重排后容易闪出旧字幕；第五版1.2的四帧异段字幕作为此项回归检查案例。

### 9.3 绑定真实语义

每张图用稳定的插图编号（如S01）对应完整口播原文。给用户的素材映射表严格只有“插图编号”和“对应口播”两列，按口播顺序排列；不得用摘要、省略号或“开头两句”代替原文。图片及套版文件名带相同编号，用户按编号查找即可。收到配音后也不向这张表追加时码；字幕制作单独处理时间。更换图片前核对画面、小标题、出处、字幕，防止相邻段落串位。

交付前把全部场景做成带ID联系表，并在约360×640或390px宽的整屏预览中检查。检查的是手机可读性，不只是原图放大后细节漂亮。

## 10. 曼波声音、字幕和缺字补齐

### 10.1 配音

剪映“曼波讲故事”为当前声音来源。保留1.0倍原声，最终仅处理一次到原声1.25倍，默认保音高；已是1.25倍的文件直接复用，不再次乘1.25。若交付时速度来源未知，先查看记录，不盲目补变速。

配音读纯正文，不读章节、来源、URL、文件ID或制作说明。补录一段时使用同一声音与速度基准，用户试听音色、音量及句间衔接。

### 10.2 默认交付黑底字幕视频

用户完成每集最终1.25倍人声后交给Codex，Codex按真实人声定时并渲染黑底白字MP4。用户直接放入剪映的黑色字幕区，不需要逐条手动设字体和字号。漫画、封面和简介可在声音准备期间继续。

若用户先剪出视频，则以指定成片版本为时码基准。定稿用于校正字词，但不能按文字长度估算时间冒充精确字幕。没有最终人声／成片时，先交分段文字并标字幕视频待制作，不能交伪造时码视频。

### 10.3 黑底MP4默认规格

| 项目 | 默认规格 |
|---|---|
| 文件 | 每集一条“黑底字幕.mp4”，H.264，30fps；黑底、不透明，无音轨 |
| 视频画布 | 776×128px，恰好对应当前黑色字幕底条；不是整屏黑视频 |
| 背景与文字 | 背景#1C1C1C；白色Noto Sans SC Bold，设计字号44px，行高52，最多两行 |
| 条内文字框 | x24/y12/w728/h104，文字居中，与整屏模板的文字框一致 |
| 剪映放置 | 在1080×1920成片中铺到x96/y1408/w776/h128，中心点为(484,1472)；不以软件面板缩放百分数代替画布匹配 |
| 时长与起点 | 整集字幕视频从本集0秒起，长度对齐完整时间轴；没有口播时保持空黑底，不删空白导致整体错位 |
| 声音 | 不重复携带曼波配音；用户使用独立人声，避免重复播放 |
| 辅助文件 | 可保留SRT及样式参数供后续改字，不能只交SRT即标字幕完成 |

这是匹配现有模板的默认实现。用户提供不同尺寸的字幕条／工程时，用其实际黑框尺寸调整，并明确放置位置。不要默认抠黑或滤色；黑底本身就是要覆盖在现有黑框上的底色，不做透明字幕或全屏遮挡。

每条字幕按语义断句，起止有序、无错误重叠，不超出实际片长。人名、地名、典籍、引文及数字单位逐项校对。SRT如作为内部渲染输入，使用UTF-8；不把TTS同音字或漫画拟声词写进人声字幕。

渲染后至少检查：能完整解码、没有音轨、黑底与模板吻合、最长两行不溢出、字幕切换无异段闪现、首尾字没有缺失。以黑底MP4作为用户默认使用文件。

### 10.4 局部缺字幕模式

只补指定缺失区间时，交该区间的黑底字幕MP4和一条明确的放置说明，不重做全片字幕或导出成片。局部MP4自身从0秒播放，字幕放置说明必须记录其对应原片的start_s与end_s，用户将片头对齐原片start_s；辅助SRT如有，标明使用的是原片绝对时码。

例如旧EP01第1.2集缺少00:31.700至约00:42.433的字幕：若再次执行补修，局部视频长约10.733秒，放入原片31.700秒处。其余编号、顺序和边缘UI由用户处理。该任务不自动成为下一选题的待办。

### 10.5 听审边界

最终配音时码、实际转写、读音／完整性、听感分别记录。没有真实音频接收或可靠识别能力时，不写“已听过”“全部通过”。允许交用户最终试听，不能因无法听审而卡住全部漫画、封面与简介，也不重复试用已失败的ASR方案。

## 11. 每集封面与一句话简介

### 11.1 每集必交一张封面

包括只有一集的选题，每集都提供一张实际1080×1920 PNG封面。沿用视频同一模板的白底、标题区、漫画画窗、朱印、页边角色和安全区，不另造一套横版海报或不同配色版式。

每张封面针对该集内容：人物与冲突清楚，标题直接有力，漫画构图富有张力。共用片头的分集仍有各自封面，不能只把同一张图的1/3改成2/3。

### 11.2 封面构图要求

- 一眼识别主体和矛盾，不把背景、人群、道具均匀铺满。
- 根据本集选择夸张表情、极近景、仰俯视、前后景大小反差、对峙、动作将发生的瞬间或醒目证据。
- 封面标题、表情和关键道具互相加强，不同时摆多个竞争焦点；手机缩小后仍有冲击力。
- 可复用该集最有张力的漫画，但必须按封面目的检查构图，不能机械截第一帧当作完成；需要时进行局部编辑或单独生成封面漫画。
- 封面使用本集标题／钩子和清楚集号。固定黑条可放一句短封面钩子或保持空底，不烧入口播字幕、播放器按钮或时间码。
- 构图强化不能捏造本集没发生的事件。角色身份仍遵守原始参考，场景文字仍须正确且融入材质。
- 封面单独交付，不自动插进成片开头占用40秒时长；若用户明确把它当片头画面使用，再计入时长。

可用提示词结构：

```text
本集：{主题与核心冲突}，制作同模板封面用的独立漫画。
主体：{人物/道具}。最强视觉焦点：{表情/动作瞬间/证据}。
镜头：{景别与夸张透视}，通过{大小反差/对峙/动作方向}形成张力。
背景简化，手机缩小后先看到主体与冲突。角色身份参考实际传入。
不要在插画里重画外层UI；完成后用当前视频模板套版，另交1080×1920封面PNG。
```

### 11.3 每集一句话简介

每集交一句可直接粘贴的发布简介，放在该集“发布文案.txt”中。默认短而直接，点出人物、本集反差或问题，不写剧情流水账，不用“本期带你了解”凑字。

简介不是完整口播摘要，不必逐字重复标题；每集分别写，不能用同一句笼统介绍应付整组选题。简介不写“咕咕嘎嘎 × Doro × 曼波”等角色名单。默认不附账号装修、标签清单或多套备选，除非用户另行要求。

本项目不再制作或提供音效、配乐和轻动效，包括推荐、搜索词、落点表、动画参数、关键帧或混音建议。漫画中的动作、速度线、构图张力和拟声文字属于静态画面表达，可继续使用。

## 12. 每期实际工作流

| 阶段 | 输入 | Codex实际输出 | 可以并行准备的工作 |
|---|---|---|---|
| A 接续 | PRD、资产包、用户选题或定稿 | 简短资产确认和当前任务范围 | 缺件不影响的资料整理 |
| B 选题与定稿 | 本期主题、资料线索 | 40秒内分集稿、各集结尾、独立／共用片头、来源表 | 共用模板与字体检查 |
| C 分镜与cos | 确认稿、角色原图 | 镜头表、本期选角和服装设定 | 用户配音 |
| D 漫画生产 | 镜头表、参考图 | 实际漫画、必要局部修图、联系表 | 无依赖的其他镜头与套版准备 |
| E 套版与发布素材 | 漫画、模板、分集定稿 | 场景图／分层资产、角色透明层、每集封面和一句话简介 | 根据最终人声处理字幕 |
| F 字幕 | 最终1.25倍人声或当前视频 | 每集已排版黑底字幕MP4、放置说明、未确认字词 | 已批准图片继续复用 |
| G 用户剪辑 | 统一交付文件夹与映射 | 用户操作剪映；Codex按需补素材 | 不自行启动GUI整片制作 |
| H 审查与补修 | 用户指定最新成片 | 按时间的问题清单、必要局部替换 | 不重开已通过内容 |

这是一条有交付的流程，不是要求执行到每一步都向用户重新请示。只有内容选择确实未定、用户要求看纠偏或缺少决定性输入时才询问；提问前先完成能确定的工作。

新建选题在投入批量生成前应有可执行稿件。已收到批准稿或用户明确要求开做时，直接制作，不以再次确认模板为由停工。

## 13. 新电脑接续与效率

### 13.1 新电脑首次同步与开工

唯一接续仓库：https://github.com/KAtOReNA7/tiktok ，常用分支：`main`。用户已授权将本项目接续资料和后续已确认更新同步到该仓库；当前环境权限仍需遵守。

1. 先检查用户指定目录。尚无此仓库时，在合适的父目录执行 `git clone https://github.com/KAtOReNA7/tiktok.git`，打开生成的 `tiktok` 文件夹作为项目根。若同名目录已有文件，检查是否同一仓库，不能覆盖旧项目；可选一个新的相邻目录克隆。
2. 已有仓库时，先执行 `git remote -v`、`git status --short`、`git branch --show-current` 核实目标与工作区，再执行 `git fetch origin`。位于 `main` 且工作区干净时，用 `git pull --ff-only origin main` 同步。不要使用强制推送、`reset --hard`、`clean -fd` 或静默覆盖来解决同步问题。
3. 本地有修改时，先保留用户改动。能独立继续时，从 `origin/main` 建立单独工作分支／工作树；发生同文件冲突则先准备合并方案，不擅自丢弃任何一边。不要自动 stash 未知用户改动，也不要把其他选题文件一并提交。
4. 同步后先读 `AGENTS.md`、`START_HERE.md`、本版PRD、`PROJECT_STATE.md`、本期明确任务和 `spec/`，实际查看Doro原图和布局参考。不要把候选提案当作已批准制作任务。
5. 记录当前 `git rev-parse --short HEAD`；存在Python时运行 `python tools/validate_handoff.py` 检查接续文件、图片与两列映射模板。Windows可用 `py -3 tools/validate_handoff.py`。缺少Python时可按 `manifest.json` 检查文件，不为单次校验安装完整开发框架。
6. 确认本任务实际需要的图片生成／参考编辑、Figma、FFmpeg/ffprobe和中文粗体能力。优先用户指定的image2／image2.5路线；只记录实际可见工具，不能通过提示词宣称已切换模型。
7. 所有路径以仓库根为基准，不绑定旧D盘或用户名。已有底板、参考图和规范已随Git克隆，新电脑缺剪映工程不会阻塞独立素材制作。最终交付仍放本地 `交付/选题ID_短名/`，不压缩。

如果不能访问GitHub，明确说明未能确认最新提交；现有快照可继续用户已授权且不依赖最新规则的工作，不得冒称已同步。

### 13.1.1 后续更新直接回写仓库

- 接手新会话或新选题时同步一次；不要在每张图生成前重复拉取。任务中途不擅自替换已锁定的定稿或素材版本。
- 已确认的通用规则改动直接修改 `PRD_V7.md`，同时更新受影响的 `START_HERE.md`、`spec/`、`episode/`；不另建长期生效的零散补丁。
- 本期稿件、映射、审片结论保存在 `episodes/{选题ID}/`，更新 `PROJECT_STATE.md` 区分“已完成、待确认、待补输入”；通用变更记入 `CHANGELOG.md`。
- 将需要跨电脑使用的固定模板和参考图保留在 `assets/`、`refs/`。新生成大批漫画、配音、成片和剪映缓存默认仍在本地交付目录；本轮仓库不伪装备份了这些文件。用户指定需要跨机同步的实际素材时，按范围纳入并更新清单。
- 更新仓库文件后运行 `python tools/validate_handoff.py --refresh` 刷新接续清单，再运行不带参数的校验。此脚本只读取和校验明确的项目路径，不上传素材。
- 提交前查看 `git diff`、`git diff --cached` 和文件列表。只暂存本次项目文件，不使用不加检查的 `git add .`，不提交登录凭证、个人数据或临时缓存。
- 在 `main` 且工作区无其他人改动时，提交本次已确认更新并 `git push origin main`。远端若前进，先fetch并合并／rebase自己的已提交改动；有冲突时解决后再正常推送，不force。
- 如仓库策略要求PR，走项目分支和PR；如当前环境无写权限，保留已经完成的本地更改，说明准确阻塞。只有远端返回成功、且读回对应提交后，才能报告“已同步GitHub”。
- 每次交付给用户仓库链接、简短改动说明和提交号；默认不再重做接续下载包。此为任务执行时的同步规则，不宣称已部署无人值守的后台同步服务。

无需重新跑旧A/B，不安装未经需要的第二套框架，不重新听审旧测试片，不因为第一次打开文档就重做Figma。

### 13.2 时间和失败处理

已发生测试：早期约40分钟的14.57秒测试；约63分钟的40秒预览；约102分钟的两条12.63秒A/B短片。第三轮未取得AI视频，听审仍未完成。这些结果说明旧流程成本偏高；没有足够遥测把总耗时归因于Ultra或中等推理。

提效优先级：减少GUI操作 → 复用模板与资产 → 先分镜减少重画 → 批量处理确定性文件 → 只修有证据的问题。提高推理档位不是解决操作等待的默认办法。

同一GUI入口连续失败两次或约3分钟没有进展，保留必要截图与具体缺口，交用户接手该一步，继续独立工作；不要原样重试几十次。生成任务按实际状态等待，不额外每步固定停顿，不高频轮询。

阶段计时只保留一份，记录准备、资料／文稿、分镜、生图等待、修图、套版、字幕、检查与交付；可拆主动工作与工具等待，不可测量部分写unknown。重叠任务不能简单相加当墙钟总耗时。不要为省事承诺尚未验证的“每期20分钟”或“效率提升80%”。

执行中的简短更新说明完成数量、具体阻塞、下一步，不用长篇重复计划。充分检查后交付，不追加无关的全片导出、模板测试和第二轮风格实验。

## 14. 文件和数据约定

### 14.1 统一位置，按选题和集号命名

所有生成的交付素材放在项目根目录下固定的 `交付/` 内。优先复用用户已指定的统一交付目录，不分散到桌面、下载目录、多个临时runs目录。使用普通文件夹，不生成ZIP、RAR或7z。

以下是下一选题的命名示例，不代表已选择EP02或已生成文件：

```text
交付/EP02_选题短名/
  00_交付清单.md
  00_共享素材/
    EP02_共用片头_口播_v01.txt      # 仅启用共用片头时
    EP02_共用片头_画面_v01.png      # 实际已制作时
    EP02_角色叠加层_v01.png
  01_第01集_本集短题/
    EP02_P01_口播稿_v01.txt        # 含本集采用的开头、正文、结尾
    EP02_P01_封面_v01.png
    EP02_P01_发布文案_v01.txt      # 标题＋一句话简介
    EP02_P01_黑底字幕_v01.mp4
    EP02_P01_字幕放置说明.txt
    EP02_P01_素材映射.csv
    漫画原图/
      EP02_P01_S01_事件短名_v01.png
    套版场景图/
      EP02_P01_S01_事件短名_套版_v01.png
  02_第02集_本集短题/
    EP02_P02_封面_v01.png
    EP02_P02_发布文案_v01.txt
    EP02_P02_黑底字幕_v01.mp4
    ...
  90_历史版本/                    # 被替换文件保留在此，不与当前使用文件混放
```

文件名使用“选题ID_集号_镜头ID（适用时）_用途_版本”，让用户只看名字即可知道归属。编号补零，版本统一v01/v02；不要连续使用“最终版、最终修改版、最终最终版”。

一份 `00_交付清单.md` 列出每集标题、实测时长／待测、片头模式、封面、简介、黑底字幕视频与对应素材路径，以及尚未完成项。新版替换后更新当前路径，旧文件放90_历史版本，不能让用户猜哪份能用。

工作中的资料、分镜、生成日志与辅助SRT可保存在 `episodes/{episode_id}/`，最终用户要用的文件统一出现在交付目录。不为形式完整创建空MP4或不存在的占位文件；不把本机临时路径当作新电脑已拥有的资源。

### 14.1.1 最少工作记录

保留一份brief、一份来源表、制作所需分镜和一份production_log。给用户的素材映射单独保持两列“插图编号、对应口播”；分镜用于设计镜头，不能把其制作字段全塞入交付映射。提示词、参考用途、版本、简短QA和阶段计时集中记录，不重复建复杂表。仅收到最终声音后，才在字幕制作文件中记录实际时码与时间基准。音效落点表、配乐表、轻动效文件不再创建。

### 14.2 标识与最小字段

- `episode_id`：选题或系列标识，例如EP02；分集另设part，不能用字幕序号代表镜头ID。
- `shot_id`：稳定语义ID，例如EP02_S03；重绘另加版本，不换掉它和口播的对应关系。
- 素材映射表仅包含两列：插图编号、对应口播。每行按剪辑顺序写明插图编号及完整定稿原文。
- 映射表不包含时间、路径、章节、版本、复用、UI／字幕／角色、状态或备注等列，不预留空列；配音完成后也不追加这些列。不另交复杂版映射表。
- 图片及套版文件名使用同一插图编号；文件版本、缺件及必要图层说明写在00_交付清单.md。清单中的路径相对项目根，不提供不存在的占位路径。
- 字幕时码只在最终音频／成片确定后写入字幕制作文件；局部补字幕的起点与终点写在字幕放置说明中，不混入插图与口播映射。
- 引用素材说明原始身份参考、风格参考与本期生成版本；不靠模型“记住了”保存项目。

## 15. 验收与最小补修

### 15.1 素材验收

| 项目 | 通过依据 |
|---|---|
| 内容对应 | 图像动作、人物、数字单位与定稿段落一致 |
| 角色身份 | 原始参考对照；表情变化允许，身份结构不漂移 |
| 漫画表现 | 主次清楚、景别和动作有变化、相邻镜头不重复骨架 |
| 图内文字 | 文本正确且与材质透视相融，台词指向清楚 |
| 模板 | 坐标稳定、标题和出处清晰、关键字底色正确 |
| 字幕分层 | 场景图黑条为空，不带上一段字幕，不重复烘焙 |
| 页边角色 | 外缘锚点正确、透明真实、父级遮罩不误裁 |
| 手机阅读 | 全屏缩小后焦点、脸、关键字和两行字幕可读 |
| 可替换性 | 插图编号与图片文件名一致，两列映射中的口播原文完整；换图保留编号 |
| 分集完整性 | 默认每集含头尾不超过40秒；每集独立结尾，独立或已说明共用片头 |
| 黑底字幕视频 | 实际MP4存在并可解码、黑底白字、无音轨、时长和放置位置正确；仅SRT不算完成 |
| 封面 | 每集一张同模板1080×1920封面；构图有张力且对应本集 |
| 一句话简介 | 每集各一条可粘贴文本，不以角色名单代替 |
| 文件夹交付 | 同一选题文件集中在统一目录，命名含选题／集号／用途／版本，无压缩包 |

### 15.2 成片技术检查

用户发成片后，以其明确指定的最新版为唯一基准。检查字幕漏字／空白、极短异段字幕闪现、画面和章节／出处错配、旧编号、角色误裁、黑帧、文件解码、有效音轨及片尾完整性。

对疑似字幕接缝逐帧定位。第五版已经出现0.13秒、4帧的旧字幕串段，粗略每几秒抽图可能漏掉；无需把全片每一帧都写进长报告，但须覆盖字幕切换状态与关键接缝。

技术范围与创意范围分开：用户认可的节奏和片头片尾不因审片被改写。0.1—0.3秒句间空白先结合配音判断，不能一律报成整句漏字幕。最后一句字幕先消失时核对尾音，不擅自把无字收尾判错。

导出目标为1080×1920、30fps、H.264/AAC。用户没有提供工程时，可以检查成片并交局部素材，不冒称修改了剪映草稿。全片解码、响度或波形扫描不能证明实际读音／听感已经通过。

### 15.3 状态与交付

素材制作、字幕、成片和听审分开记：`READY`表示对应素材可交付；`PARTIAL`写明缺哪项；`BLOCKED`写明具体依赖；`NOT_REQUESTED`表示本轮未要求。不能因为音频未验收，把已经完成的漫画说成未制作；也不能因为有MP4就写全部QA通过。

最终向用户简短交付：实际产物、修改范围、可点击文件、明确缺件、必要人工一步。不要默认发布、购买额度、追加下一组选题。

## 16. 复用策略

### 一直复用

白底UI、字体颜色与文字框、朱印、刻度、字幕样式、角色边缘锚点、已验证的装配方式、文件字段和QA方法。

### 每题更换

选题与观点、分集定稿与结尾、历史材料、分镜、三位cos、漫画情境、标题关键词、段落标题、短出处、配音和黑底字幕视频，以及每集封面与一句话简介。优秀共用片头按本选题复用。

### 按需新增

必要表情、道具、局部特写、简短漫画台词及封面所需的构图编辑。模板不能让所有题目变成相同站姿与构图；复用的是生产结构，不是把旧图换一句字。

用户在新电脑调整出的有效字幕样式或UI修复，保存为新模板资产并记录改动；不能默默覆盖旧母版。没有资产更新时，新电脑以包内V1.1为基准，不假设第五版成片中的人工改动已经回写Figma。

## 17. GitHub接续仓库的真实范围

包含：主PRD、README／AGENTS接手入口、当前进度、变更记录、清单校验工具、已存的武松定稿与审片记录、待确认的商鞅提案、启动指令、当前模板参数、字幕样式参数、空白本期brief和CSV字段、已存在的底板、三位秦装独立角色及整屏透明层、Doro两张原始参考、V1.1布局截图、第五版人工布局参考、四张漫画语言参考、原Figma归档参数以及文件校验清单；V7.1改为普通文件夹交付，不新增压缩文件。

仓库内选题状态以PROJECT_STATE.md为准，武松已完成素材制作及用户剪辑，商鞅提案未获批准，不能自动重跑。

不包含：用户电脑上的最新剪映工程、第三方登录或付费额度、所有秦始皇重绘原图、最终独立配音、未交付的字幕修复结果、下一选题的定稿和新cos。包内漫画语言截图只作参考，不当成可直接替换的新题素材。

旧V1.1中湘山图的Doro脸曾被指出不准，因此没有把那两张旧漫画作为推荐身份母图纳入交付。V1.1布局截图只看版式；角色身份始终看用户原图。第五版1.3的外层UI有已知裁切问题，漫画参考截图只看画窗内，不复制其错误UI。

## 18. 合并来源与被覆盖的旧要求

| 来源 | 继承 | 被本版覆盖／不再执行 |
|---|---|---|
| V3—V5及旧操作记录 | 已验证素材思路、来源记录、真实计时和能力边界 | 旧慢速声音、从零全片GUI、程序化整片为必选路线 |
| V6／V6.1 | 复用、局部恢复、不要无限找入口 | A/B交付、Seedance镜头、旧测试稿、十二卡全部验证 |
| 白底单格V1.1与素材生产指令 | 精确布局、颜色、分层、Figma资产、Doro原图 | 全部无字生图、固定24张图、固定秦始皇章节号 |
| Doro纠偏补丁 | 原图实际输入、身份优先、只修受影响区域 | 把一次纠偏确认变成每张图强制审批 |
| COMIC-VISUAL-01 | 分镜变化、三类文字、台词、手机阅读和局部替换 | 第1集三张替换时间不作为新题任务 |
| 第三版修正版审片 | 文字双重验收、评论者构图区分、素材分层、语义绑定 | 已解决错位不再列为新待办；轻动效建议现已取消 |
| 第四版拆集方案 | 主题拆分、完整句界、新时间轴、集内重编号 | 不复制其建议片头片尾覆盖用户第五版；三集不固定 |
| 第五版审片与用户最新回复 | 四帧串字幕检查、透明层误裁检查、只补指定区间 | 默认仅交SRT被黑底字幕MP4覆盖；用户负责编号、顺序与边缘UI |
| 用户本轮七条调整（V7.1） | 40秒内、片头共用例外、独立结尾、同模板封面、一句话简介、统一文件夹 | 取消音效／配乐／轻动效及方案；取消仅SRT交付与素材压缩包；废止时长不限的旧默认 |

本版建立的是可接续的生产规范，不宣称各项素材或新机操作已全部通过，也不保证爆款。后续有真实发布数据时，围绕开场流失、平均观看时长、完播和有内容的讨论调整；不同内容的三集不是严格控制变量的A/B实验。

## 19. 新电脑可直接执行的启动指令

```text
先按第13.1节克隆或安全同步 https://github.com/KAtOReNA7/tiktok 的main分支，读取AGENTS.md、START_HERE.md、PRD_V7.md、PROJECT_STATE.md，使用V7.2规则及现有模板资产执行当前明确任务；不要回退旧PRD，也不要把待确认提案当成定稿。完成本次已授权的规范与接续更新后，按第13.1.1节提交回仓库并回报提交号。

你负责文稿辅助、分镜、漫画和套版素材、每集封面、一句话简介、黑底字幕视频。我负责剪映配音、试听与装配，以及编号顺序和UI人工调整。

每集默认含片头、正文、片尾在40秒以内。内容多则按独立问题分集，默认分别写开头结尾；如果某个优秀开头适合该选题全部分集，采用shared_topic共用片头，只制作一份共用资产。每集仍独立结尾、封面和简介。不能再次加速1.25倍人声来压时长。

漫画按第7节执行：镜头和动作有变化，场景文字进入材质，短台词有指向，Doro真实输入用户原图。每集封面使用同一视频模板，漫画构图有张力、突出本集冲突；一句话简介针对本集，可直接粘贴发布。

收到最终1.25倍人声／当前成片后，默认交已排版黑底白字MP4字幕条，按当前模板为776×128、30fps、无音轨，覆盖到x96/y1408的黑框。只交SRT不算完成；辅助SRT按需保留。局部补字幕只做指定范围，并写明应对齐的原视频时间点。

不制作或提供音效、配乐、轻动效及其推荐方案；不启用AI视频、剪映助手、旧A/B或从零GUI整片制作。

所有最终素材统一放在项目根的交付/选题ID_短名/内，按集号分文件夹，文件名含选题ID、集号、用途和版本。交付清单指明当前使用文件；不生成压缩包，不分散到临时目录。

素材映射表严格只交“插图编号、对应口播”两列，按口播顺序填写完整原文，不加入时间或其他元数据，也不预留空列。图片文件名带对应编号；字幕时码在收到最终声音后单独制作。

已有选题和定稿就直接推进实际素材；只有选题先完成40秒内分集稿；没有选题先给约6个高知名度、强反差且有具体支撑的候选。常规实现不反复问确认，完成授权范围即交真实文件和必要缺件说明。
```
